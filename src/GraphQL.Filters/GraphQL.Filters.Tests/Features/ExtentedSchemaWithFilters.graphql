query FilterType($name: String!) {
    __type(name: $name) {
        name
        description
    }
}

query FieldEnum($name: String!) {
    __type(name: $name) {
        enumValues   {
            name
        }
    }
}